/**
 * Replace this with tests that encode intent.
 *
 * A cost or accounting figure needs a hand-computed expectation, not a snapshot
 * of whatever the code happened to produce.
 */

import { describe, expect, test } from 'bun:test'

describe('dsh-example', () => {
  test('the skeleton is wired up', () => {
    expect(true).toBe(true)
  })
})
