# @reedchan7/dsh-example

English | [中文](README_CN.md)

One line about what Example does, then what the user sees and where.

## Install

```sh
make install PLUGINS=dsh-example
```

Restart `dsh web` and hard-refresh the browser.

## Notes

Replace this section with the behaviour a reader cannot guess from the UI: what
is estimated, what is out of scope, and which routes the host half serves.
